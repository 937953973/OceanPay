<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
         http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">

  <!-- 1. 字符编码过滤器 (使用原生Servlet API的Filter) -->
  <filter>
    <filter-name>encodingFilter</filter-name>
    <filter-class>com.oceanpay.filter.SetCharacterEncodingFilter</filter-class> <!-- 这个类需要我们自己创建 -->
    <init-param>
      <param-name>encoding</param-name>
      <param-value>UTF-8</param-value>
    </init-param>
  </filter>
  <filter-mapping>
    <filter-name>encodingFilter</filter-name>
    <url-pattern>/*</url-pattern> <!-- 过滤所有请求 -->
  </filter-mapping>

  <!-- 2. 默认欢迎页面 -->
  <welcome-file-list>
    <welcome-file>index.jsp</welcome-file>
  </welcome-file-list>

  <!-- 3. (可选) 在这里以传统方式声明Servlet，如果你不使用@WebServlet注解的话 -->
  <!--
  <servlet>
      <servlet-name>ProductListServlet</servlet-name>
      <servlet-class>com.oceanpay.servlet.ProductListServlet</servlet-class>
  </servlet>
  <servlet-mapping>
      <servlet-name>ProductListServlet</servlet-name>
      <url-pattern>/product/list</url-pattern>
  </servlet-mapping>
  -->
</web-app>